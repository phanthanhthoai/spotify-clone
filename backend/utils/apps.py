from django.apps import AppConfig

#Utils la noi chua cac ham hoac class dung chung 
class UtilsConfig(AppConfig):
     name = 'utils'
     verbose_name = 'Utils'
     default_auto_field = 'django.db.models.BigAutoField'